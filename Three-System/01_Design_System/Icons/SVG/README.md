# SVG Icons

SVG icons are the preferred format for scalable, crisp graphics across platforms.

## Best practices

- Keep icon shapes simple and consistent.
- Use a single path or minimal groups when possible.
- Optimize SVGs for file size and remove unnecessary metadata.

## Usage

Use SVG icons in responsive UI, and keep them aligned to the system's visual grid and stroke weight.
